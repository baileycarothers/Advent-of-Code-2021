#include <iostream>
#include <fstream>
#include <string>

int main() {
    FILE* inputFile;
    inputFile = fopen("input.txt", "r");
    FILE* outputFile;
    outputFile = fopen("output.txt", "w");

    int increasesFound = 0;
    string numFound;
    while(getline(inputFile, numFound) {

    };
    return 0;
}